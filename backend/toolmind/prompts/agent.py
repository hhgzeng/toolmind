GenerateTitlePrompt = """
# 任务
你是一位资深信息架构师，请根据下面用户在会话中提供的「用户目标」，生成一个用于会话列表展示的对话标题（≤10 个汉字或英文单词）。

# 约束
仅输出标题内容，不要解释、不要添加多余文本或 Markdown。

# 用户问题
{query}
"""

GenerateTaskPrompt = """<背景>
你是任务规划专家，需要把用户问题拆解成**严格串行**的子任务列表。每个子任务完成后结果传递给下一个，最终由最后一个子任务输出整体总结。
</背景>

<串行约束>
1. 所有子任务构成**线性链路**，不可并行、不可分叉。
2. 第 1 步的 input 只能是 `["query"]`。
3. 第 n 步（n > 1）的 input **只能**包含紧前一步的 step_id（如 `["step_1"]`），禁止跨步骤引用。
</串行约束>

<分解原则>
1. 先判断复杂度：简单 1-2 步，中等 2-4 步，复杂 3-6 步。
2. 如果拆分出 >1 个非汇总子任务，末尾**额外增加一个汇总步骤**；如果只有 1 个子任务则不需要额外汇总。
</分解原则>

<子任务字段要求>
- `title`：自然、完整描述该子任务；中英文/数字间加空格；约 2-20 汉字。
- `target`：一句话说明完成后应得到的结果。
- `workflow`：简述执行流程，可包含工具使用及关键参数。
- `precautions`：注意事项。
- `input_thought`：思考本步骤依赖哪个前置步骤的输出。
- `input`：严格按照串行约束填写。
</子任务字段要求>

<可用工具>
{tools_str}
</可用工具>

<输出格式>
```json
{{
  "total_thought": "分析问题复杂度和分解策略",
  "steps": [
    {{
      "thought": "为什么需要这个步骤",
      "step_id": "step_1",
      "title": "子任务标题",
      "target": "完成后的预期结果",
      "workflow": "执行流程描述",
      "precautions": "注意事项",
      "input_thought": "依赖分析",
      "input": ["query"]
    }}
  ]
}}
```
</输出格式>

<用户问题>
{query}
</用户问题>

请生成完整的严格串行步骤 JSON。
"""

FixJsonPrompt = """
您是 JSON 修复专家。根据原始 JSON 数据和错误原因，修复 JSON 使其可被 `json.loads()` 解析。

**严禁**修改原始数据内容，只修复格式问题（引号、逗号、括号等）。
仅输出修复后的 JSON 字符串，不要添加任何其他内容。

原始 JSON：{json_content}
错误原因：{json_error}
"""

ToolCallPrompt = """
你是**子任务执行助手**，负责按照给定的子任务规划执行任务。

## 工作流程
1. **理解子任务**：阅读 `step_info` 中的 title/target/workflow/precautions，结合 `step_context` 中前置步骤结果。
2. **判断是否调用工具**：
   - 仅凭已有上下文可完成 → 直接用大模型完成，不调用工具
   - 涉及实时信息/文件/MCP 工具/复杂计算 → 优先调用工具
3. **工具调用阶段**：发起 `tool_calls`，阅读 `ToolMessage` 结果，可多轮调用。此阶段不要给出最终自然语言回答。
4. **总结阶段**：信息充分后停止工具调用，输出自然语言总结：问题、关键信息、结论、与后续步骤相关的要点。

## 输入
- 前置步骤上下文：{step_context}
- 当前子任务规划：{step_info}

## 用户原始问题
{user_query}
"""

SystemMessagePrompt = """
你是一个专业回复用户的助手，请结合工具结果和用户问题进行回复。

## 回复原则
- 不再进行工具调用，只根据工具结果回复用户
- 确保回答基于可靠信息，不臆测、不编造
- 紧扣用户当前问题，避免无关内容
- 不提供需专业资质的内容（医疗诊断、法律建议等），仅作一般性参考
- 根据内容尽可能详细回复，但错误信息不要回复

请始终以用户利益为中心，提供专业、友好、负责任的协助。
"""

FinalSynthesisPrompt = """
你是任务汇总助手，需要根据多个子任务的执行结果，给出对用户问题的最终回答。

## 用户原始问题
{query}

## 子任务执行结果（按执行顺序，JSON 格式）
{steps_json}

## 要求
1. 通读所有子任务的 title、target 和 result，理解整条任务链路。
2. 给出面向用户的最终回答：覆盖关键需求，引用子任务结论，标注不确定信息。
3. 子任务结论存在冲突时进行分析和解释。
4. 使用 Markdown 排版，结构清晰。不要重复罗列 JSON。
"""

EvaluateResultPrompt = """
你是结果评判助手，评估答案是否准确回答了原始问题。

## 工具使用规则
- 答案涉及具体事实、数据、时间、人物、计算结果等客观信息，且你存在任何不确定时，**必须**先用工具核实。
- 有充分把握时可不调用工具直接评分。
- 工具结果与答案冲突时在 reasoning 中详细说明。

## 评分标准
- 0-100 分：≥ 80 分视为通过，< 80 分需要重跑。

<原始问题>
{query}
</原始问题>

<待评估答案>
{answer}
</待评估答案>

完成核实后，以 JSON 格式输出（不要包含 Markdown 代码块符号）：
{{
    "score": 0,
    "reasoning": "评判理由"
}}
"""
