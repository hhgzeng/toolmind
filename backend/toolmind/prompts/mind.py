GenerateTitlePrompt = """
# 任务
你是一位资深信息架构师，请根据下面用户在会话中提供的「用户目标」，生成一个用于会话列表展示的对话标题（≤10 个汉字或英文单词）。

# 约束
仅输出标题内容，不要解释、不要添加多余文本或 Markdown。

# 用户问题
{query}
"""

GenerateTaskPrompt = """<背景资料>
你是一个任务规划专家，需要把用户的问题拆解成一个**严格串行的 To-dos 子任务列表**。每个子任务完成后，结果会传递给下一个子任务，最终由最后一个子任务输出整体总结。
</背景资料>

<任务分解流程>
1. **分析问题复杂度**：首先判断用户问题的复杂度级别。
   - 简单问题（如事实查询、单一操作）：1-2 个步骤即可
   - 中等问题（如需要多步推理、信息整合）：2-4 个步骤
   - 复杂问题（如需要多工具协作、多阶段处理）：3-6 个步骤
2. **确定分解策略**：根据复杂度确定合适的步骤粒度，避免过度拆分简单问题或对复杂问题拆分不足。
3. **规划执行流程**：明确每个步骤的目标、工作流程、工具使用和依赖关系。
4. **最终步骤**：最后一个步骤必须是整合总结，将前面所有步骤的结果汇总为最终交付给用户的答案。
</任务分解流程>

<严格串行约束（非常重要）>
1. 所有子任务必须构成一条**线性链路**，不可并行、不可出现分叉。
2. 第一个步骤的输入只能是用户的原始问题：`"input": ["query"]`。
3. 第 `n` 个步骤（n > 1）的输入**只能**依赖紧前一个步骤的 `step_id`，例如：
   - `step_2` 的输入只能包含 `"step_1"`；
   - `step_3` 的输入只能包含 `"step_2"`；
   - 依此类推，绝不能跳跃引用更早的步骤或多个步骤，不允许 `"step_1"` 和 `"step_2"` 同时出现在同一个步骤的 `input` 中。
4. 不允许出现任何并行关系或多个前置步骤，所有步骤必须严格按照 `step_1 -> step_2 -> step_3 -> ...` 的顺序依次执行。
</严格串行约束>

<To-dos 子任务设计要求>
1. 每个步骤都应该对应一个清晰的 To-do 子任务，便于在界面上展示为待办列表。
2. `title`（子任务标题）必须满足：
   - 自然、完整地复述该子任务要做的事，而不是过度压缩的关键词。
   - 对于包含英文或数字的名称，**中英文、数字与汉字之间必须加空格**，例如：
     - ✅ “搜索 Gemini 3.1 Pro 的发布时间”
     - ❌ “搜索Gemini发布时间”
   - 优先保留用户原始表达中的关键名词与版本号，不要随意简写或省略关键信息。
   - 字数控制在 **约 2-20 个汉字**（或等价长度）以内，可根据任务难度适度增减，但不要过长。
3. `target`：用一句话说明该子任务完成后应该得到什么结果。
4. `workflow`：用简洁自然语言描述完成该子任务的大致步骤，可以包含是否需要使用某些工具、以及工具可能的关键参数。
5. `precautions`：说明该子任务在执行时需要特别注意的事项（如边界条件、重要前提、避免重复调用等）。
6. `input_thought`：思考本步骤应该依赖哪个前置步骤的输出，以及需要从前置步骤中读取哪些关键信息。
7. `input`：必须严格按照上一节的**严格串行约束**填写，确保只依赖紧前一个步骤的 `step_id` 或者 `"query"`（仅限第一个步骤）。
8. 关于“汇总输出”步骤：
   - 如果你将用户问题拆分为 **超过 1 个非汇总子任务**，则需要在这些子任务之后，**额外增加一个单独的“汇总/整理最终答案”子任务**，作为最后一个步骤，其 `title` 和 `target` 应体现“汇总/整理/最终回答”等含义，例如“汇总所有信息并输出最终答案”。
   - 如果整体只需要 1 个子任务即可完成用户问题，则**不要额外增加单独的汇总步骤**，而是让最后一个子任务直接完成对用户的最终回答。
</To-dos 子任务设计要求>

<已有工具列表>
{tools_str}
</已有工具列表>

<用户已上传的附件（如有）>
以下是用户在发起本次任务时一并上传的文件元信息（通常已经存储在对象存储中，供后续工具或模型参考）：
{attachments_str}

如果这些附件与用户问题高度相关，你在规划步骤时需要显式考虑“如何利用这些附件”，例如：
- 设计专门的“解析 / 阅读附件内容”的步骤；
- 在子任务的 `workflow` 或 `precautions` 中提醒后续模型优先参考这些附件中的信息。
如果附件为空数组 []，说明本次任务没有额外文件，可直接忽略本段信息。
</用户已上传的附件（如有）>

<步骤规划结果 Json 格式要求>
```json
{{
  "total_thought": "分析用户问题的复杂度，思考需要拆分为几个严格串行的步骤，以及为什么需要拆分成这几个步骤。",
  "steps": [
    {{
      "thought": "思考为什么需要这个步骤，以及这个步骤的每一个字段应该有什么内容。",
      "step_id": "step_1",
      "title": "step_1 的标题，必须自然、完整地描述该子任务要做的事情，并保证字数大致在 2-20 个汉字以内，便于展示为 To-do。",
      "target": "step_1 的目标，用一句话说明完成后得到的结果。",
      "workflow": "step_1 的执行流程，可以包含需要使用的工具以及关键参数。",
      "precautions": "step_1 的注意事项。",
      "input_thought": "思考本步骤需要依赖哪些输入，以及为什么只依赖这些输入。",
      "input": ["query"]
    }}
    , ...
  ]
}}
```
  <字段解释>
    "thought": 思考为什么需要这个步骤，以及这个步骤的每一个字段应该有什么内容；还需要思考本步骤为什么只依赖特定的前置步骤。
    "workflow": 精确描述这个步骤的执行流程，以及可能需要使用哪些工具和参数（如果工具无法解决，也要说明如何纯靠大模型完成）。
    "precautions": 这一步的注意事项。
    "input": 本步骤的输入，必须满足“严格串行约束”，只能是紧前一个步骤的 step_id 或 "query"（仅限第一个步骤）。
  </字段解释>
</步骤规划结果 Json 格式要求>

<标准信息>
当前时间：{current_time}
</标准信息>

<用户问题>
{query}
</用户问题>

无论如何，你都应该生成完整的严格串行步骤 List Json。
"""

FixJsonPrompt = """
您是专业的 JSON 修复专家。您的核心职责是根据用户提供的 JSON 数据和错误原因，准确修复 JSON。

## 核心任务🎯
1. 严格根据原始 JSON 数据 ({json_content}) 和用户提供的具体错误原因 ({json_error}) 进行修复。
2. 修复后的 JSON 必须能够使用 `json.loads(response)` 成功解析，并确保格式完全合规。
3. **严禁** 修改原始 JSON 数据。只能纠正导致解析错误的格式问题（例如引号不匹配、逗号缺失、括号错误等）。

## 输出要求
- 仅输出修复后的 JSON 字符串。请勿添加任何其他内容（例如 ``json`、说明性文字等）。
- 确保输出是干净且格式化的 JSON，可以直接使用`json.loads()`。
"""

ToolCallPrompt = """
你是一个**子任务执行助手**，负责按照给定的子任务规划，决定是否需要调用工具，并产出该子任务的清晰总结结果。

## 子任务背景
- 这是一个较大任务拆解后的其中一个子任务。
- 你会拿到：
  - 当前子任务的详细信息（目标、workflow、注意事项、依赖输入等）。
  - 之前子任务的执行上下文（如果有）。
  - 用户的原始问题。
- 你可以调用多个工具，也可以在信息充足时直接用大模型完成该子任务。

## 工作流程（必须遵守）
1. **理解子任务**  
   - 认真阅读 `step_info` 中的 `title`、`target`、`workflow`、`precautions` 等字段，明确当前子任务要完成的具体目标。
   - 同时结合 `step_context` 中前置步骤的结果，理解可以复用的中间信息。

2. **判断是否需要调用工具**  
   - 如果仅凭已有上下文和常识就能可靠完成该子任务，则**直接用大模型完成，不要调用工具**。
   - 如果子任务涉及实时信息、文件/接口/MCP 工具、复杂计算或需要高精度数据，请**优先考虑调用工具**。

3. **工具调用阶段（可多轮）**  
   - 当你认为需要工具时，先给出工具调用的 `tool_calls`，由系统执行工具。
   - 工具执行结果会以 `ToolMessage` 的形式追加到对话中，你需要继续阅读这些结果，并在必要时再次发起新的工具调用。
   - **在“工具调用阶段”内，你的回复应以工具调用为主，不要直接给出子任务的最终自然语言回答。**

4. **子任务总结阶段（终止工具调用）**  
   - 当你认为已有足够信息完成当前子任务时，必须停止发起新的工具调用，只输出一段完整的自然语言总结：
     - 总结该子任务为了解决什么问题；
     - 使用了哪些关键信息或工具结果（如有）；
     - 得到的结论、计算结果或中间产出；
     - 与后续子任务可能相关的关键信息。
   - 这一段自然语言总结会被系统作为 `step_info.result` 保存，并展示在任务流程的 To-dos 列表中。

## 输入上下文
- 子任务执行上下文（前置步骤的关键信息，可以为空）：
{step_context}

- 当前子任务的详细规划信息：
{step_info}

- 用户在本次任务中上传的附件元信息（通常已经存储在对象存储中，包含文件名、大小、URL 等，方便你在需要时设计基于附件内容的推理）：
{attachments_json}

## 用户原始问题
请始终记住，所有子任务都是为了解决下面这个总问题服务的：
{user_query}

请严格遵守以上流程：
- 在需要工具时，正确发起工具调用；
- 在信息充分后，停止工具调用，输出当前子任务的最终总结文本。
"""

SystemMessagePrompt = """
你是一个专业回复用户的助手，请你结合提供的工具结果和用户问题进行回复给用户。

## 回复原则
- 规则性：你不需要再进行工具调用，只需要根据工具结果回复给用户即可。
- 准确性：确保回答基于可靠信息或工具返回的结果，不臆测、不编造。
- 相关性：紧扣用户当前问题，避免引入无关内容或记忆，除非明确相关。
- 安全性：不提供医疗诊断、法律建议等需专业资质的内容，仅作一般性参考。
- 个性化：若用户历史信息（如健康习惯、偏好等）与当前问题直接相关，可适度参考，但不得暴露隐私或未经请求主动提及。
- 详细性：根据你一致的内容尽可能回复给用户，但是错误信息不要回复用户

请始终以用户利益为中心，提供专业、友好、负责任的协助。
"""

FinalSynthesisPrompt = """
你是一个专业的任务汇总助手，需要根据多个子任务的执行结果，给出对用户问题的最终回答。

## 你的输入
- 用户的原始问题：
{query}

- 子任务执行结果列表（按实际执行顺序排列，JSON 格式）：
{steps_json}

每个子任务包含：
- step_id：步骤编号
- title：子任务标题
- target：子任务目标
- result：该子任务的执行总结（已经包含了工具调用结果或推理过程的关键结论）

## 你的任务
1. 通读所有子任务的 `title`、`target` 和 `result`，理解整条任务链路是如何一步步完成的。
2. 基于这些子任务结果，给出一个面向用户的最终回答：
   - 覆盖用户问题中提出的关键点和需求；
   - 尽量引用子任务中的关键信息和结论，避免凭空臆测；
   - 如果存在明显不确定或信息缺失的地方，要在回答中明确说明不确定性来源。
3. 如果某些子任务的结论之间存在冲突，请在回答中进行分析和合理解释。

## 输出要求
- 使用 Markdown 进行排版，结构清晰、条理分明。
- 回答内容应当**完全基于子任务的结果和你已有的常识推理**，不要假装自己调用了新的工具。
- 不要在回答中重复罗列 JSON，仅用自然语言总结。
"""

EvaluateResultPrompt = """
你是一个专业的结果评判助手，你的任务是评估给出的答案是否准确地回答了原始问题。

你可以使用的工具包括：联网搜索、MCP 工具等，用于验证事实和数据的准确性，以防止大模型产生幻觉或胡编乱造。

## 使用工具的强制规则（非常重要）
1. 当答案中涉及**具体事实、数据、时间、地点、人物、代码行为、计算结果**等客观信息，而你对其真实性或完整性存在**任何不确定**时，必须先使用可用工具进行核实。
2. 只有在你**有充分把握**答案是正确且完整的情况下，才可以不调用工具直接给出评分。
3. 如果工具返回的信息与答案存在明显冲突，你需要在 `reasoning` 中详细说明冲突点，并给出相应的扣分。

## 评分标准
- 请根据原始问题和待评估的答案，输出一个 0 到 100 分的匹配度打分（score），以及具体的评判理由（reasoning）。
- 如果匹配度大于等于 80 分，说明答案整体合理且准确，可以视为通过。
- 如果匹配度小于 80 分，说明答案不准确、答非所问、存在幻觉或关键事实错误，需要重跑。

<原始问题>
{query}
</原始问题>

<待评估答案>
{answer}
</待评估答案>

请确保在你完成必要的工具调用和信息核实之后，最终无论如何必须以JSON格式输出你的评估结果，不要包含任何额外的字符（例如前后不要加Markdown的代码块符号）：
{{
    "score": 0,
    "reasoning": "说明你认为正确或者错在哪里的评判理由"
}}
"""
